<!-- 3 update the user profile page to call the helper so that the user's profile image is shown on the page -->
<%= profile_image_for(@user) %>
<h1><%= @user.name %></h1>


<%= link_to 'Edit Account', edit_user_path(@user) %>
<%= link_to 'Delete Account', @user,
						method: :delete,
						data: { confirm: 'Are you want to delete your account?' } %>